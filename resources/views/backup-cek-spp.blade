<!DOCTYPE html>
<html lang="id" 
    x-data="{ 
        darkMode: localStorage.getItem('theme') === 'dark' 
    }" 
    x-init="$watch('darkMode', val => localStorage.setItem('theme', val ? 'dark' : 'light'))"
    :class="{ 'dark': darkMode }">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cek SPP - Ponpes Al-Amin</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        darkbg: '#0b1120',
                        darkcard: '#1e293b'
                    }
                }
            }
        }
    </script>
    
    <style>
        body { font-family: 'Inter', sans-serif; scroll-behavior: smooth; }
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="bg-gray-50 dark:bg-darkbg text-gray-800 dark:text-gray-100 min-h-screen flex flex-col items-center p-6 transition-colors duration-500">

    <button @click="darkMode = !darkMode" class="fixed top-4 right-4 z-50 p-3 rounded-full bg-white dark:bg-darkcard shadow-lg border border-gray-200 dark:border-gray-700 transition-all">
        <i class="fa-solid" :class="darkMode ? 'fa-sun text-yellow-400' : 'fa-moon text-gray-500'"></i>
    </button>
    
    <div class="w-full max-w-4xl bg-white dark:bg-darkcard rounded-xl shadow-lg overflow-hidden border border-gray-100 dark:border-gray-800 animate__animated animate__fadeInDown transition-colors duration-500">
        
        <div class="bg-emerald-700 dark:bg-emerald-900 p-6 text-white flex flex-col sm:flex-row items-center justify-between gap-4">
            <div class="flex items-center gap-4">
                <img src="{{ asset('images/logo-ponpes.png') }}" alt="Logo Ponpes" 
                     class="h-14 w-14 object-contain rounded-full bg-white p-1 shadow-md">
                <div class="text-center sm:text-left">
                    <h1 class="text-xl font-bold uppercase tracking-wide">Ponpes Al-Amin</h1>
                    <p class="text-emerald-100 text-xs mt-1">Sistem Pengecekan SPP Mandiri</p>
                </div>
            </div>
            <img src="{{ asset('images/logo-yayasan.png') }}" alt="Logo Yayasan" class="h-12 opacity-50 hidden sm:block">
        </div>

        <div class="p-8">
            <form action="{{ url('/cek-spp') }}" method="GET" class="flex flex-col sm:flex-row gap-3 mb-8">
                <div class="flex-1">
                    <label class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase mb-1 ml-1">Masukkan NIS Santri</label>
                    <input type="text" name="nis" placeholder="Masukkan NIS untuk mencari..." 
                           class="w-full border-2 border-gray-200 dark:border-gray-700 dark:bg-gray-800 dark:text-white rounded-lg px-4 py-3 focus:outline-none focus:border-emerald-500 transition-all duration-300 shadow-inner" 
                           value="{{ request('nis') }}" required>
                </div>
                <div class="flex items-end">
                    <button type="submit" class="w-full sm:w-auto bg-emerald-600 hover:bg-emerald-700 text-white font-bold px-8 py-3 rounded-lg transition-all shadow-md active:scale-95">
                        <i class="fa-solid fa-magnifying-glass mr-2"></i> CARI DATA
                    </button>
                </div>
            </form>

            @if(request()->has('nis'))
                @if($santri)
                    <div class="mb-6 grid grid-cols-1 sm:grid-cols-3 gap-4 bg-emerald-50 dark:bg-emerald-900/10 p-5 rounded-xl border border-emerald-100 dark:border-emerald-800/30 animate__animated animate__fadeInUp shadow-sm">
                        <div class="flex flex-col">
                            <span class="text-emerald-600 dark:text-emerald-500 text-[10px] font-black uppercase tracking-widest">Nama Santri</span>
                            <span class="text-gray-800 dark:text-gray-200 font-bold text-lg capitalize">{{ $santri->nama_santri }}</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-emerald-600 dark:text-emerald-500 text-[10px] font-black uppercase tracking-widest">NIS</span>
                            <span class="text-gray-800 dark:text-gray-200 font-bold text-lg">{{ $santri->nis }}</span>
                        </div>
                        <div class="flex flex-col">
                            <span class="text-emerald-600 dark:text-emerald-500 text-[10px] font-black uppercase tracking-widest">Kelas / Asrama</span>
                            <span class="text-gray-800 dark:text-gray-200 font-bold text-lg">{{ $santri->kelas }}</span>
                        </div>
                    </div>

                    <div class="overflow-x-auto animate__animated animate__fadeIn animate__delay-1s">
                        <table class="w-full text-left border-collapse">
                            <thead>
                                <tr class="bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 text-[10px] font-black uppercase tracking-widest">
                                    <th class="p-4 border-b dark:border-gray-700">Periode Tagihan</th>
                                    <th class="p-4 border-b dark:border-gray-700 text-right">Nominal</th>
                                    <th class="p-4 border-b dark:border-gray-700 text-center">Status</th>
                                    <th class="p-4 border-b dark:border-gray-700 text-center">Tgl Lunas</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-100 dark:divide-gray-800">
                                @forelse($tagihan as $t)
                                <tr class="hover:bg-emerald-50/30 dark:hover:bg-emerald-900/10 transition-colors duration-200">
                                    <td class="p-4 font-bold text-gray-700 dark:text-gray-300">
                                        {{ $t->bulan }} {{ $t->tahun }}
                                    </td>
                                    <td class="p-4 text-right text-gray-800 dark:text-gray-200 font-mono font-bold text-sm">
                                        Rp {{ number_format($t->nominal, 0, ',', '.') }}
                                    </td>
                                    <td class="p-4 text-center">
                                        @if($t->status_bayar == 'LUNAS')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-[10px] font-black bg-emerald-100 text-emerald-700 dark:bg-emerald-900/40 dark:text-emerald-400">
                                                <i class="fa-solid fa-circle-check mr-1 text-xs"></i> LUNAS
                                            </span>
                                        @elseif($t->status_bayar == 'PENDING')
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-[10px] font-black bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-400 animate__animated animate__pulse animate__infinite">
                                                <i class="fa-solid fa-clock mr-1 text-xs"></i> VERIFIKASI
                                            </span>
                                        @else
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-[10px] font-black bg-rose-100 text-rose-700 dark:bg-rose-900/40 dark:text-rose-400">
                                                <i class="fa-solid fa-circle-xmark mr-1 text-xs"></i> BELUM BAYAR
                                            </span>
                                        @endif
                                    </td>
                                    <td class="p-4 text-center text-gray-500 dark:text-gray-400 text-[10px]">
                                        {{ $t->tanggal_lunas ? \Carbon\Carbon::parse($t->tanggal_lunas)->translatedFormat('d M Y') : '--' }}
                                    </td>
                                </tr>
                                @empty
                                <tr>
                                    <td colspan="4" class="text-center p-12 text-gray-400 dark:text-gray-600 italic text-sm">
                                        <i class="fa-solid fa-folder-open text-3xl mb-3 block"></i>
                                        Tidak ada riwayat tagihan untuk NIS ini.
                                    </td>
                                </tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>
                @else
                    <div class="bg-rose-50 dark:bg-rose-900/20 border-l-4 border-rose-500 p-6 rounded-xl animate__animated animate__headShake">
                        <div class="flex items-center gap-4">
                            <div class="bg-rose-500 text-white p-3 rounded-full shadow-lg">
                                <i class="fa-solid fa-user-slash text-xl"></i>
                            </div>
                            <div>
                                <h3 class="text-rose-800 dark:text-rose-400 font-bold uppercase text-xs tracking-widest">Data Tidak Ditemukan</h3>
                                <p class="text-rose-700 dark:text-rose-300 text-sm mt-1">NIS <span class="font-black underline">{{ request('nis') }}</span> tidak terdaftar dalam sistem kami. Pastikan nomor yang anda masukkan benar.</p>
                            </div>
                        </div>
                    </div>
                @endif
            @endif
        </div>

        <div class="px-8 pb-8 flex flex-col sm:flex-row justify-center gap-4">
            <a href="{{ url('/') }}" class="inline-flex items-center justify-center gap-2 bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 text-gray-700 dark:text-emerald-400 font-bold px-6 py-3 rounded-xl transition-all shadow-sm active:scale-95 text-sm">
                <i class="fa-solid fa-house"></i> BERANDA
            </a>
            <a href="https://wa.me/6285773353921" target="_blank" class="inline-flex items-center justify-center gap-2 bg-emerald-100 dark:bg-emerald-900/30 hover:bg-emerald-200 dark:hover:bg-emerald-800/50 text-emerald-700 dark:text-emerald-400 font-bold px-6 py-3 rounded-xl transition-all shadow-sm active:scale-95 text-sm">
                <i class="fa-solid fa-comment-dots"></i> HUBUNGI BENDAHARA
            </a>
        </div>

        <div class="bg-gray-50 dark:bg-gray-800/50 p-4 border-t border-gray-100 dark:border-gray-700 text-center">
            <p class="text-gray-400 dark:text-gray-500 text-[9px] uppercase tracking-[0.2em] font-black">
                &copy; {{ date('Y') }} Ponpes Al-Amin - Integrity in Finance
            </p>
        </div>
    </div>

</body>
</html>